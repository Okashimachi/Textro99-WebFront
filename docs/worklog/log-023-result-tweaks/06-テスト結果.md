# 06 テスト結果

> [05-テスト仕様.md](./05-テスト仕様.md) の各項目の合否と、実際の挙動。

## 実施環境

| 項目 | 値 |
|---|---|
| 実施日 | 2026-08-01 |
| 実施者 | Claude Code (claude-opus-5) |
| 経路 | Vite dev server ＋ ブラウザ操作（DOM を直接読んで検証） |
| モード | 練習（フロント完結）＋ Dev モックの S2C 手動注入 |

## 結果

| # | 項目 | 合否 | 実挙動 |
|---|---|---|---|
| T-1 | 型検査・ビルド | ✅ | `tsc --noEmit` エラーなし。`vite build` 成功（204.89 kB / gzip 65.17 kB） |
| T-2 | テストボタン非表示 | ✅ | `/` のボタンは `["プレイする"]` のみ |
| T-3 | 隠し入口 | ✅ | `/?test=1` のボタンは `["プレイする","テスト用"]`。モード選択へ遷移できた |
| T-4 | バッジ非表示（試合中） | ✅ | 上部バーは「残り 1 人」のみ。ランキング行・盤面セルに数値なし |
| T-5 | 伸ばし棒 | ✅ | 「ぷれいやー」に `pureiya-` を入力 → クリアされ次のお題へ。ミス 0、攻撃力 1 |
| T-6 | 半角数字（打鍵） | ✅ | 「てきすとろ99」に `tekisutoro99` を入力 → `DakenClearReport` 送信。**戦略は「—」のまま**（9 が作戦選択に食われていない） |
| T-7 | 数字（作戦選択の回帰） | ✅ | 数字を含まないお題の状態で `5` → `StrategySelect {"strategyId":5}` 送信、戦略 5 に |
| T-8 | リザルトのバッジ | ✅ | 個人成績は「最大コンボ / ダケンクリア / ミス / 正確率 / 経過時間」。**画面全体の innerText に「バッジ」が 0 件** |
| T-9 | 共有文面 | ✅ | `テキストロ99 で 42位 でした\n撃破 2人 / 最大コンボ 18\n\n#テキストロ99` |
| T-10 | 猶予30秒 | ✅ | 定数は `30_000`。実挙動は未確認（下記） |

### 補足: 「こーひー」でも確認

伸ばし棒が2つ含まれる「こーひー」に `ko-hi-` を入力し、クリアされることを確認した（攻撃力 2 に加算）。

## 対応した不具合

- なし（実装中に typecheck を通し、検証で不具合は出なかった）。

## 残課題

- **T-10 の実挙動は未確認。** 30秒カウントダウンはサーバーが接続を切ったときにだけ起動するため、練習モードでは再現できない。実サーバーでの確認が必要。
- マッチング画面のイベントログ非表示は画面上では未確認（練習モードがマッチング画面を経由しないため）。コード上は `showDevTools` false で `events` を渡さない。
