# 05 テスト仕様

> 何を・どう確認するか。期待結果まで書く。

## 前提

| 項目 | 値 |
|---|---|
| proto ver | 本リポ同梱 `src/proto/types.ts`（v0.1.1 相当・変更なし） |
| サーバーモード | 使用せず（練習＝フロント完結モード＋ Dev モックの S2C 手動注入） |
| 経路 | `?test=1` →「テスト用」→「練習（フロント完結）」→ 名前入力 |

## 確認項目

| # | 対象 | 項目 | 手順 | 期待結果 |
|---|---|---|---|---|
| T-1 | 全体 | 型検査・ビルド | `npm run typecheck` / `npm run build` | エラーなし |
| T-2 | 5 | テストボタン非表示 | `/` を開く | タイトルに「プレイする」だけが出る |
| T-3 | 5 | テストモードの隠し入口 | `/?test=1` を開く | 「テスト用」が出て、モード選択へ進める |
| T-4 | 3 | バッジ非表示 | 練習開始 → 画面全体 | 上部バーに「撃破 N バッジ」が無い。ランキング行に数値が無い。盤面セルに数値が無い |
| T-5 | 4 | 伸ばし棒 | お題「ぷれいやー」で `pureiya-` を打つ | クリアされ次のお題へ。ミス 0 |
| T-6 | 4 | 半角数字（打鍵） | お題「てきすとろ99」で `tekisutoro99` を打つ | クリアされる。**作戦は未選択のまま**（数字が StrategySelect に食われない） |
| T-7 | 4 | 数字（作戦選択の回帰） | 数字を含まないお題の状態で `5` を押す | `StrategySelect{strategyId:5}` が送信され、戦略が 5 になる |
| T-8 | 3 | リザルトのバッジ | Dev モックで `GameOver (脱落 42位)` を注入 | 個人成績に「最終バッジ」が無い。画面全体に「バッジ」の文字が無い |
| T-9 | 1 | 共有文面 | 同上の状態で「結果をポスト」の `title` を読む | `テキストロ99 で 42位 でした / 撃破 2人 / 最大コンボ 18 / #テキストロ99` |
| T-10 | 2 | 猶予30秒 | `SESSION_END_COUNTDOWN_MS` を確認 | `30_000` |

## 未実施（今回のスコープ外）

- 実サーバー接続時の確認（`--mode match`）。特に **T-10 の30秒カウントダウンは、サーバーが接続を切った時にだけ起動する**ため、練習モードでは動作確認できない（定数の確認のみ）。
- マッチング画面のイベントログ非表示は、練習モードがマッチング画面を経由しないため画面上では未確認（コード上は `showDevTools` false で `events` を渡さない）。
