# 06 テスト結果

ブラウザ（localhost:5173）でモック S2C を流し確認。

| 受け入れ条件 | 結果 |
|---|---|
| S2C 受信で画面が正しく遷移する | ✅ title → (MatchStart) inMatch → (GameOver) result。脱落時 spectating も導出可 |
| リザルトから再マッチングで待機へ戻れる | ✅ result → (再マッチング) matchmaking |

修正記録: rematch 後もリザルトに留まる不具合を検出し、`result` 条件を `intent === "idle"` に限定して解消。再検証で title→inMatch→result→matchmaking を確認。
