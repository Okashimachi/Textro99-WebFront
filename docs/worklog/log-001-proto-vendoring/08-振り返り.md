# 08 振り返り

> うまくいった点・詰まった点・上流ドキュメントに反映すべき知見。

## うまくいった点

- 作ったばかりの運用（develop/feature ブランチ・コミット規約・worklog）を初回タスクで一通り実地に回せた。
- 手コピーの弱点（版ズレ・二重管理）を、vendor ヘッダ＋`src/proto/README.md` の同期手順＋log-001 の判断記録で運用として塞げた。
- 取り込みの正確性を diff で検証し、CRLF 差という偽陽性を切り分けられた。

## 詰まった点 / 反省

- 検証 diff が CRLF 差で全行差分になり、大きな出力を2回出してしまった（→ 07 の改善案）。
- Vite プロジェクト未作成のため tsc 型チェックができず、コンパイル検証は後続タスク送りになった。

## 上流ドキュメントへの還元候補

| 反映先 | 内容 |
|---|---|
| Textro99-Proto | `ts/types.ts` の行末が CRLF。3言語ミラーの一致検証を機械化するなら改行コードを統一（`.gitattributes` で `text=lf` 等）しておくと差分比較が楽 |
| 本リポ docs/rules | 「vendored proto の同期手順」は `src/proto/README.md` に置いたが、取り込み方式が確定したので rules 側にも一行索引を足すか検討（今は README 参照で足りる） |
| 本リポ | Vite プロジェクトのスキャフォールド（tsconfig の `@/*` パスエイリアス設定含む）を次タスクで起票し、型チェックを回せるようにする |
