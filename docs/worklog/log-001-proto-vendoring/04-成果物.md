# 04 成果物

> 追加/変更したファイル、実装した機能、ドキュメント。差分の要点。

## 変更ファイル

| ファイル | 追加/変更/削除 | 要点 |
|---|---|---|
| `src/proto/types.ts` | 追加 | Proto v0.1.0 (`f47fcf5`) の `ts/types.ts` を丸写し。先頭に vendor ヘッダ（取得元・版・改変禁止）を付加 |
| `src/proto/README.md` | 追加 | 現在版の記録・編集禁止原則・import 集約方針・proto 更新時の同期手順・未取り込み事項（ローマ字/npm移行） |
| `docs/worklog/log-001-proto-vendoring/*` | 追加 | 本作業ログ一式 |

## 実装/変更の要点

- 型は **proto 正典の写し**であり、web 側では編集しない（vendor ヘッダと README に明記）。
- web 実装からの import は `src/proto/types.ts` に集約（将来 npm 化しても付け替えを局所化）。
- ローマ字データ・npm 移行は今回スコープ外として README に残置。

## コミット / PR

- `feat: proto の TS 型を手コピーで取り込み`（`src/proto/`）
- `docs: worklog log-001 を起票`（本ログ）
- ブランチ: `feature/proto-vendoring` → PR は `develop` ベース（マージは人間）
