# 06 テスト結果

ブラウザ（localhost:5173）でモックボタンを操作し確認。

| 受け入れ条件 | 結果 |
|---|---|
| 受信のたびに最新 state/JSON が見える | ✅ Welcome→MatchStart→Delta の順に state タブが即時更新 |
| トグルで表示/非表示できる | ✅ ▼/▲ で最小化・復帰 |

副次的に #5 reducer を実挙動で確認:
- Welcome: `selfPlayerId=p1`、events に記録
- MatchStart: players 99件・activeDaken・parameters を反映
- PlayerListDelta: p2 `alive=false`、p3 `stackRatio=0.9`/`badgeCount=2`、並び順保持、`aliveCount=98`
