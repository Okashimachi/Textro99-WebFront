# 06 テスト結果

ブラウザ（localhost:5173）で inMatch にし、window へ keydown を発火して確認。

| 受け入れ条件 | 結果 |
|---|---|
| Enter で AttackRequest が送られる | ✅ `AttackRequest {consumedCombo:0}` を送信試行 |
| 数字で StrategySelect が送られる | ✅ `5`/`7` で `StrategySelect {strategyId}`、選択戦略=7 |
| 文字キーが判定層に渡る（送信は DakenClearReport 単位） | ✅ `k`/`a` は `onCharKey` へ（最終打鍵=a）。**C2S として送信されない**ことを送信ログで確認 |

補足: 未接続のため送信ログは `×`（観測のみ）。実サーバ接続時は `→`。#8 判定本体は上流ブロック中のため未接続。
