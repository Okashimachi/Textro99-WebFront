# 03 判断と決定

- `GameOver` DTO を表示するだけ。数値はサーバー由来（rank/koCount/finalBadgeCount/typingStats）。正確率は totalDakenCleared/(cleared+miss) の**表示計算**のみ（戦闘・勝敗ロジックではない）。
- `rank===1` で優勝表示、それ以外は順位＋脱落表示に出し分け。
- 再マッチング（rematch 意図＋Join 送信）／タイトルへ（backToTitle）は既存 lifecycle アクションに配線。
