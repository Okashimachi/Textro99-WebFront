# 06 テスト結果

| 受け入れ条件 | 結果 |
|---|---|
| ローカルサーバへ接続し Welcome を受信してログ出力 | ⏳ 実サーバ未起動のため統合時に確認。`on(MessageType.Welcome, ...)` で購読可能な設計を型で担保 |
| 任意の C2S を Envelope 形式で送信 | ✅ `send(type, payload)` が `{type,payload}` を JSON 送信（型検証済み） |
| 未知 type を安全に無視（クラッシュしない） | ✅ `handleRawMessage` が未購読/未知 type を無視（設計・レビューで確認） |
| `npm run typecheck` | ✅ 通過 |

備考: 実 WebSocket サーバ（Go `--mode solo/match`）との疎通は #7 前後の統合フェーズで実施する。
