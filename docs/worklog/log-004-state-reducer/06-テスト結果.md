# 06 テスト結果

| 受け入れ条件 | 結果 |
|---|---|
| 各 S2C を state に反映できる（型安全） | ✅ 全 S2C を型付きで畳み込み。`npm run typecheck` 通過 |
| Delta 適用でプレイヤー一覧が正しく更新 | ✅ `applyPlayerDelta`（undefined=変化なし・順序保持）。#6 デバッグペイン統合でモック Envelope により実挙動確認予定 |
| 戦闘数値のローカル算出が無いこと | ✅ サーバー値を格納するのみ。派生計算なし（レビュー観点で確認） |

備考: reducer 単体はサーバー無しでも #6（RawStateDebugPane）にモック S2C を流して検証できる。実サーバ疎通は統合フェーズ。
