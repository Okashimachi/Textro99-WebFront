# 04 成果物

> 追加/変更したファイル、実装した機能、ドキュメント。差分の要点。

## 変更ファイル

| ファイル | 追加/変更/削除 | 要点 |
|---|---|---|
| `src/screens/MatchResultScreen.tsx` | 追加 | 決着レイアウト。ランキング(large) / 敵の状況(large) / リザルト の3カラム＋決着バナー |
| `src/state/viewModel.ts` / `src/state/reducer.ts` | 変更 | `defeatedBy`（自分を倒した KoNotified の写し）を追加。`GameOver` DTO に撃破者が入らないため保持が必要。MatchStart でリセット |
| `src/dev/mockMessages.ts` | 変更 | 「KoNotified (自分が撃破される)」「GameOver (脱落 42位)」を追加（dev 検証用） |
| `src/screens/ResultBoard.tsx` | 追加 | 中央〜右のリザルトパネル。トドメを刺した相手（DEFEATED BY）、順位・KO・バッジ・タイピング統計、終了カウントダウン（進捗バー）、再マッチング/タイトル導線。0 到達で `onSessionEnd` を1回だけ発火 |
| `src/components/hud/LiveRanking.tsx` | 変更 | `size="large"` を追加（行を大きく・上位3位を色帯・順位のバンプ演出）。既定の compact は従来どおり |
| `src/components/PlayerGrid99.tsx` | 変更 | `size="large"` を追加（セル・バッジ数を大きく、危険域の相手を脈動）。既定の compact は従来どおり |
| `src/screens/ResultOverlay.tsx` | 追加 → 削除 | 当初モーダルとして実装したが、指示 #4 で決着レイアウトに置き換えたため削除 |
| `src/screens/ResultScreen.tsx` | 削除 | 排他フェーズ時代のリザルト画面。`ResultBoard` に置き換え |
| `src/screens/sessionEnd.ts` | 追加 | `SESSION_END_COUNTDOWN_MS = 15_000` と `isConnectionFinished(status)`。「試合完全終了＝サーバー切断」の判定をここ1箇所に閉じる |
| `src/screens/lifecycle.ts` | 変更 | `ScreenPhase` から `"result"` を削除。`dismissedResult` を最優先の離脱判定に。`gameOver` があっても `matchId` がある限り `spectating` を返す |
| `src/screens/useScreenPhase.ts` | 変更 | GameOver をローカルへ焼き付ける `matchResult`（＝セッションの封印）を追加。封印中は開始ゲート・intent リセット・フェーズ導出をすべて抑止し、観戦に固定。各アクションで封印解除 |
| `src/screens/ScreenRouter.tsx` | 変更 | `result` ケースを削除。`spectating` で、自分の結果があれば `MatchResultScreen`、無ければ従来の観戦画面を描画。`matchResult` / `sessionEndDeadlineMs` / `onSessionEnd` を受け取る |
| `src/net/connection.ts` | 変更 | `autoReconnect` を可変にし `setAutoReconnect(enabled)` を追加（false で予約済みの再接続タイマーも解除） |
| `src/App.tsx` | 変更 | `joinArmedRef` による `MatchmakingJoin` の送信制御、GameOver での封印（join 解除＋autoReconnect 停止）、終了カウントダウンの期限管理、`exitToTitle` での明示切断 |
| `src/screens/index.ts` | 変更 | `MatchResultScreen` / `ResultBoard` / `SESSION_END_COUNTDOWN_MS` を公開 |

## 実装/変更の要点

1. **リザルトは決着レイアウト**（モーダルではない）。フェーズは `spectating` のまま、試合画面のレイアウトを差し替える。ランキング・敵の状況・戦況ログはブロードキャストで更新され続け、試合中に「お題・NEXT・作戦」が占めていた中央〜右がリザルトになる。3カラムとも主役の大きさで、ランキングは上位3位を色帯＋順位バンプ、敵の盤面は危険域の相手を脈動させる（表示のみ）。
2. **自動で次試合へ行かない**を3重で担保。
   - 参加意思（`joinArmed`）が立っているときしか `MatchmakingJoin` を送らない。GameOver で降ろす。
   - GameOver で `setAutoReconnect(false)`。サーバーが切ったらそのまま切れたままにする。
   - 封印中は `MatchStart` を受けても画面を試合中へ戻さない（サーバーが同一接続で次試合を始める実装への保険）。
3. **セッションの終端は1本の出口**。「サーバー切断 → 15秒 → `exitToTitle`」も「タイトルへ」ボタンも同じ `exitToTitle` を通り、必ず `connection.disconnect()` してから `stage="title"` に戻る。
4. **再マッチング**は参加意思と autoReconnect を立て直してから `connection.reconnect()`（1接続=1試合の契約どおり接続を張り直す）。

## コミット / PR

- 未コミット（このログの記入時点）。ブランチ `feature/matchmaking-waiting-info`。
