# 06 テスト結果

> [05-テスト仕様.md](./05-テスト仕様.md) の各ケースに対する結果。

| 実施日 | proto ver | サーバーモード |
|---|---|---|
| 2026-07-26 | 手コピー版 `src/proto/types.ts` | mock（フロント完結・ブラウザ検証） |

## 結果

| # | 結果 | 実際の挙動 | 備考 |
|---|---|---|---|
| U1 | ✅合格 | 起動時 `画面: inMatch / 接続状態: フロント完結（ローカル）/ 自分: you / 生存: 1`、お題「kyoto」表示 | StrictMode により初回 Welcome が2回イベントログに出る（dev のみ・無害） |
| U2 | ✅合格 | 「kyoto」＋「打鍵 0/5」表示 | — |
| U3 | ✅合格 | 各文字入力で打鍵進捗が増加（緑ハイライト） | — |
| U4 | ✅合格 | 「kyoto」打ち切りで `DakenClearReport{mock-1,isMiss:false,missCount:0}` 送出、次のお題「tori」が自動出題 | — |
| U5 | ✅合格 | 誤字 `z` で「ミス 1」、位置は進まず。続けて `tori` を打ち切ると `DakenClearReport{mock-2,isMiss:true,missCount:1,elapsedMs:12910}` | — |
| U6 | ✅合格 | クリア2回でコンボ2、Enter で `AttackRequest{consumedCombo:2}` 送出しコンボ0 | 送信ログの `×` は mock で WS未送信＝正常（onOutbound で模擬サーバーが応答） |
| U7 | ⏭部分 | mock ループは動作。server 切替は Origin 許可待ちのため実接続確認は保留（log-013（別ブランチ feature/server-integration）） | — |
| U8 | ✅合格 | 型検査通過 | — |
| U9 | ✅合格 | `npm run build`（tsc -b && vite build）成功 | — |

## 総括

- **合否: フロント完結モードの「出題→表示→タイピング→ダケン判定→次出題→コンボ→攻撃消費」ループが全て動作。** 理想フローの④タイピング判定(#8)が埋まり、フロントは単体で完結してテスト可能になった。
- **申し送り**:
  - server モードの実接続確認は log-013（別ブランチ feature/server-integration） の Origin 許可待ち。許可後に server モードで再検証する。
  - かな入力の本判定は Proto の RomajiTable 取り込み後（`judge.ts` を差し替え）。
  - StrictMode の二重 Welcome、mock 送信ログの `×` 表示は dev 上の見え方の話で機能影響なし。必要なら後日整える。
