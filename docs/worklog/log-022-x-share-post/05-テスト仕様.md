# 05 テスト仕様

> 何を・どう確認するか。期待結果まで書く。

## 前提

| 項目 | 値 |
|---|---|
| proto ver | 本リポ同梱 `src/proto/types.ts`（v0.1.1 相当・変更なし） |
| サーバーモード | 使用せず（練習＝フロント完結モード＋ Dev モックの S2C 手動注入） |
| 経路 | タイトル →「テスト用」→「練習（フロント完結）」→ 名前入力 → RawStateDebugPane の「Dev モック」から `GameOver` を注入 |

## 確認項目

| # | 項目 | 手順 | 期待結果 |
|---|---|---|---|
| T-1 | 型検査 | `npm run typecheck` | エラーなし |
| T-2 | ボタンの表示位置 | `GameOver (優勝)` を注入し決着画面を出す | `ResultActions` 内の「再マッチング / タイトルへ」の下・右寄せに「結果をポスト」が出る。既存の操作導線を押し出さない |
| T-3 | 文面（脱落時） | `GameOver (脱落 42位)` を注入し、ボタンの `title` を読む | `テキストロ99 で 42位 でした\n撃破 2人 / 最大コンボ 18\n\n#テキストロ99` |
| T-4 | 文面（優勝時） | `GameOver (優勝)` を注入し、ボタンの `title` を読む | `👑 テキストロ99 で優勝しました！\n撃破 8人 / 最大コンボ 45\n\n#テキストロ99` |
| T-5 | intent URL の組み立て | 生成される URL を確認 | `https://x.com/intent/post?text=...` の形。本文が URL エンコードされ、改行が `%0A` で保持されている |
| T-6 | 共有URLの環境変数化 | `VITE_SHARE_URL` 未設定で起動 | `url` パラメータが付かない（例外・空 url にならない） |
| T-7 | 秘密情報 | 差分を確認 | `.env` を含まない。本番URL・トークンが差分に無い |

## 未実施（今回のスコープ外）

- 実サーバー接続時の決着フローでの確認（`--mode match`）。
- X 側での実投稿。投稿画面が開く直前までが今回の要件のため、投稿確定は検証していない。
- 文字数（重み280）超過時の挙動。バリデーション自体を実装していない（[03-判断と決定.md](./03-判断と決定.md) D-5）。
